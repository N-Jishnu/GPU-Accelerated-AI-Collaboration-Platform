# Optional dependencies for real model providers.
# Install only if you want non-mock generation providers.

# Local text-to-speech provider (SLINGSHOT_TTS_PROVIDER=pyttsx3)
pyttsx3>=2.90

# Local image generation provider (SLINGSHOT_IMAGE_PROVIDER=diffusers)
# These are heavy dependencies and may require GPU-specific installs.
diffusers>=0.30,<1.0
transformers>=4.44,<5.0
accelerate>=0.33,<1.0
safetensors>=0.4,<1.0

# Install torch according to your GPU/runtime from official instructions.
